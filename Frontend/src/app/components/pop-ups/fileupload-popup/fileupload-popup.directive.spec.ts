import { FileuploadPopupDirective } from './fileupload-popup.directive';

describe('FileuploadPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new FileuploadPopupDirective();
    expect(directive).toBeTruthy();
  });
});
